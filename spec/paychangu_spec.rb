# frozen_string_literal: true

RSpec.describe Paychangu do
  it "has a version number" do
    expect(Paychangu::VERSION).not_to be nil
  end

  xit "raises an error if a wrong currency is passed to it" do
   
  end
end
